/**
 * 与原生 refMap 保持同名导出
 */
const refMap = {
  login: '#login',
  loading: '#loading',
  auth: '#auth',
  popupCenter: '#popupCenter',
  officialAccount: '#officialAccount',
  taskReward: '#taskReward'
}

export default refMap
